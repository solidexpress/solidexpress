# Demo WebMs / captions are NEVER in git — not this tree, not the Pages repo.
# They live only on the Pages-repo GitHub Release tag `demo-movies`.
# Refresh: make movies && make publish-demo-movies
assets/movies/
*.webm
*.mp4
*.avi
*.mov
*.vtt
