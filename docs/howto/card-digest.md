# Card digest

Goal: one auto sentence on the card — type + kind + placement. Not a chat panel.

## Steps

1. Place a box.
2. Read `card_digest` on the primitive feature.

## What “good” looks like

- Digest contains `primitive` and `box`.

Film: `card_digest`.
