# Marking menu + overlapping pick

Goal: S-key (or hold-RMB overflow) lists verbs and names both overlapping picks.

## Steps

1. Select a body/face.
2. Press **S** (model context — not sketch Select).
3. The popup lists Fillet / Hole / TriBall and Face + Body rows.

Film: `marking_menu_pick`.
