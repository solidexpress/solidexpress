# Catalog fastener

Goal: M6×20 is in the base table — not a paid Toolbox tier.

## Steps

1. **Mode → Form**.
2. Look up `M6x20` in the in-base catalog.

## What “good” looks like

- Diameter 6 mm, length 20 mm.

Film: `catalog_fastener`. Kernel: `[wave4][catalog]`.
